# ── MoMo MCP Server configuration ───────────────────────────────────────────
# Copy to `.env` and fill in. NEVER commit the real .env (it is git-ignored).
# Sandbox only. No production endpoints in this repo.
#
# IMPORTANT: keep values free of inline `# comments`. This file is read both by
# python-dotenv (local) AND by `docker run --env-file` (container), and Docker
# does NOT strip inline comments — it would treat them as part of the value.
# Put comments on their own lines, as below.

# ── MTN subscription keys (from momodeveloper.mtn.com → your subscriptions) ──
# Primary key of each subscribed product. Required.
MOMO_COLLECTION_SUBSCRIPTION_KEY=
MOMO_DISBURSEMENT_SUBSCRIPTION_KEY=

# Optional: subscribed but not implemented.
MOMO_REMITTANCE_SUBSCRIPTION_KEY=

# ── API user / key (filled by scripts/provision.py — leave blank initially) ──
# The API user id IS a uuid4 generated during provisioning.
MOMO_API_USER=
MOMO_API_KEY=

# ── Environment / endpoints ─────────────────────────────────────────────────
MOMO_TARGET_ENV=sandbox
MOMO_BASE_URL=https://sandbox.momodeveloper.mtn.com
# Placeholder is fine in sandbox.
MOMO_CALLBACK_HOST=https://example.com
# Sandbox is EUR-only; production Rwanda uses RWF (see GOTCHAS.md).
MOMO_CURRENCY=EUR

# ── Agent guardrails ────────────────────────────────────────────────────────
# DRY_RUN=true is default-safe: realistic responses, zero HTTP calls.
DRY_RUN=true
# Payouts require a second confirm_payout() call with a one-time code.
REQUIRE_PAYOUT_APPROVAL=true
MAX_AMOUNT_PER_TX=100
MAX_DAILY_TX_COUNT=50
MAX_DAILY_TOTAL=1000
# Comma-separated MSISDN allowlist. Sandbox default = magic test numbers only.
MSISDN_ALLOWLIST=46733123450,46733123451,46733123452,46733123453,46733123454

# ── Local store ─────────────────────────────────────────────────────────────
MOMO_DB_PATH=./data/momo.sqlite3

# ── Client-side rate limit ──────────────────────────────────────────────────
RATE_LIMIT_PER_SEC=5
